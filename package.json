{
  "name": "pi-scheduler",
  "version": "0.3.3",
  "private": true,
  "description": "Sistema de scheduling persistente para agentes de IA en el ecosistema pi",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "check": "npm run check --workspaces",
    "test": "npm run test --workspaces",
    "clean": "npm run clean --workspaces",
    "release": "npm run build --workspaces && npm publish --workspace=pi-scheduler-core && npm publish --workspace=pi-scheduler-ext && npm publish --workspace=pi-scheduler-daemon"
  },
  "license": "MIT",
  "devDependencies": {
    "@sinclair/typebox": "^0.34.49"
  }
}
